language: java
jdk:
  - openjdk8

branches:
  only:
    - master
    - develop

before_install:
  - chmod +x gradlew

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script:
  - "export NODE_ENV=production"
  - "./gradlew clean build"
